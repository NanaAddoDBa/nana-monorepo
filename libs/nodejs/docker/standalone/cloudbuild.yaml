steps:
  - name: gcr.io/cloud-builders/docker
    args:
      - build
      - -f
      - libs/nodejs/docker/standalone/Dockerfile
      - -t
      - ${_IMAGE}
      - ${_APP_DIR}

images:
  - ${_IMAGE}
